ENTORNO

py -3.11 -m venv .venv
.\.venv\Scripts\activate

pip install streamlit
streamlit run streamlit_app/Home.py
si hay problemas con el ENTORNO
1) Ir a la carpeta del proyecto
2) Borrar el venv roto y crear uno nuevo
# Cerrar cualquier terminal que tenga el venv activado. Luego:
if (Test-Path .venv) { Remove-Item .venv -Recurse -Force }

py -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip

uvicorn app.main:app --reload --port 8000// python -m uvicorn backend.app.main:app --reload --port 8000  // http://127.0.0.1:8000/docs
pip install fastapi uvicorn[standard] sqlalchemy pydantic email-validator passlib[bcrypt] python-multipart PyJWT

//Test


python -m pip install pytest
python -m pytest -q
python -m pip install httpx

//prueba con entorno activado
python -m backend.app.seed_demo_data
pip install pydantic

// mysql

python -m pip install --upgrade pip
python -m pip install pymysql

//encriptacion 
python -m pip install "python-jose[cryptography]"
python -m pip install "passlib[bcrypt]"
